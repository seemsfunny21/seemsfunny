PEGASUS 298

- Cloud sync: detects JSONBin "Requests exhausted" quota responses explicitly.
- When JSONBin quota is exhausted, Pegasus pauses cloud sync attempts for a long quiet cooldown instead of retrying every 30-120 seconds.
- Local changes remain pending; no data is deleted or reset.
- Fetch/latest and push failures now keep the response body preview so we can distinguish quota exhaustion from wrong keys or network failures.
- The 40-minute workout guard, permanent video cache, mobile timer settings and all previous UI/data fixes are preserved.
- Desktop/mobile data is not reset; this is a cloud-quota safety patch.
