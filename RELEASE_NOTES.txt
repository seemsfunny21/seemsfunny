PEGASUS 292
- Mobile PIN auto-unlock: when the saved master key is already filled, entering the 4-digit PIN automatically runs unlock.
- The unlock button remains as fallback.
- Desktop unchanged.
- Cache bump to 292.

PEGASUS 291
- Restored mobile weekly-progress compatibility without bringing back adaptiveTypography.js.
- Mobile now defines PegasusWeeklyProgress/reconcile/repairFromLedger directly in mobile.html, so weekly set repair still works on mobile.
- Cleaned remaining static all-caps mobile labels: Βάρη, Τελευταία αποθηκευμένα κιλά, saved master-key delete text, EMS labels, day reset note, Oracle dashboard text, and Kouki day header.
- Vehicle save and Personal documents save no longer show popup alerts; they update silently with inline status.
- Vehicle service entries no longer force toUpperCase; saved text keeps the user's normal casing.
- Updated manifest documentation: Parking is now manual notes/recent locations, not geolocation tracking.
- Cache bump to 291.


PEGASUS 289
- Full static label case cleanup across project files: Greek UI labels now use first-letter uppercase/sentence case instead of all-caps static text.
- Removed adaptiveTypography.js from HTML loading and service worker cache.
- Deleted adaptiveTypography.js from the package.
- Kept weekly-progress compatibility inside app.js so counted-ledger progress/repair continues without the typography runtime.
- Bumped service worker cache to 289.

PEGASUS 285 - Greek Documents/Vehicle UI Polish + Taxisnet
- Mobile Documents and Vehicle local transfer buttons are now Greek: “Αντίγραφο” and “Επαναφορά”, fixing the bad Back/Πλάτη translation issue.
- Documents and Vehicle labels now use Greek title-style casing instead of all-caps/English labels.
- Added a Taxisnet field under Iban in Personal documents.
- Local backup/restore for documents now includes Taxisnet.
- Updated Greek status messages for local copy/restore results.
- Desktop unchanged. No new files.
- Cache bump to 285.


PEGASUS 284 - MOBILE DOCUMENTS/VEHICLE LOCAL BACKUP + RESTORE
- Added BACKUP / RESTORE buttons to mobile Έγγραφα.
- Added BACKUP / RESTORE buttons to mobile Όχημα.
- Documents backup exports/imports pegasus_user_specs locally.
- Vehicle backup exports/imports vehicle identity, dates, and service history.
- Documents and vehicle data save locally only; vehicle service logs are now also local-only.
- Restore is local-device only and does not push private document/vehicle identity to cloud.
- No new JS file added; changes are integrated into existing profile-mobile.js and car-mobile.js.
- Cache bump to 284.

PEGASUS 283 - Parking Original Manual Mode Restored

- Restored Parking mobile to the original manual text/location form from before GPS/map experiments.
- Parking again uses the simple input + recent locations list.
- Removed GPS capture, address lookup, map buttons, map thumbnails, and manual-save GPS flow from Parking.
- Parking history returns to TOP 10 recent text locations; tapping a location moves it to the top/current position.
- Parking keys are synced again like the original cloud parking flow.
- Silent weight-save behavior from 282 is preserved.
- Desktop unchanged.
- Cache bump to 283.


PEGASUS 282 - Parking 10 History + Silent Weight Save
- Mobile Parking now keeps the latest 10 saved locations instead of 5.
- Tapping/selecting any saved Parking history card promotes it to the first/current position.
- Parking keeps the v269/v279 map-opening behavior.
- Weight save no longer shows a success popup after entering weight.
- Cache bump to 282.
PEGASUS 280 - Parking Street Name Retry + v269 Map Open
- Mobile Parking keeps the v269 direct map-opening behavior from 279.
- Parking now retries reverse geocoding on open when saved items still show coordinates.
- Added stronger street/address lookup flow: Nominatim Greek, Nominatim fallback, and BigDataCloud locality fallback.
- Saved current parking and the last 5 history items can be upgraded from coordinates to street/address labels when a lookup succeeds.
- If street lookup is unavailable, coordinates remain as a safe fallback.
- Desktop unchanged.
- Cache bump to 280.

PEGASUS 275 - Parking direct map open fix
- Mobile Parking keeps manual-save coordinates behavior from 274.
- Fixed Χάρτης opening: Android now tries a Google Maps intent first so it can open the Maps app directly.
- Added safe web fallback only if the app is not opened.
- No auto location overwrite, no address lookup, no map image, no desktop change.

PEGASUS 273 - Mobile Parking GPS Coordinates + Fixed Width
- Parking mobile reverted from street/manual labels back to GPS coordinates.
- Removed the manual area input/save UI from Parking.
- Removed the orange left bar/theme that could create horizontal movement on mobile.
- Parking panel now locks horizontal overflow and uses compact fixed text sizes.
- MAP/Χάρτης buttons use stable small font sizes so text does not start large then shrink.
- Map opens with Google Maps web URL directly to avoid geo:// fallbacks that can send APK WebView to Play Store.
- Mobile only; desktop unchanged.
- Cache bump to 273.


PEGASUS 267 - MOBILE LOCAL LAST LOCATION
- Added local-only mobile last-location save.
- Mobile can ask once for permission and then update last coordinates when the mobile page opens/returns visible.
- Added Settings card to enable/update, open in map, or delete the locally saved location.
- Location keys are explicitly local-only in cloudSync and are not uploaded to cloud/exported.
- No desktop UI changes.
PEGASUS 266 - MERGED MOBILE MASTER KEY SAVE
- Mobile saved MASTER KEY support is now integrated into mobile/mobile.html unlock flow.
- Removed separate mobile/masterkey-save-mobile.js so the package does not grow with an extra module file.
- Behavior unchanged: after successful PIN + MASTER KEY unlock, mobile can ask to save only the MASTER KEY locally on that device; PIN is not saved and nothing is synced to cloud.
- Email report fixes from 265 are preserved.
- Desktop unchanged.
- Service worker cache bumped to 266.



PEGASUS 265 - EMAIL REPORT FIX + MOBILE SAVED MASTER KEY
- Fixed email report dispatch to wait for EmailJS before sending instead of failing if the CDN loads late.
- Added EmailJS/reporting support to mobile so pending reports can be sent from the mobile app too.
- Switched daily backup injection to email-safe summary mode so oversized backup JSON does not block report delivery.
- Added local-only mobile saved MASTER KEY module. After a successful PIN + MASTER KEY unlock, Pegasus asks whether to save only the MASTER KEY on that device. PIN is never saved.
- Added forget button for the saved mobile MASTER KEY.
- Cache bump to 265.

PEGASUS 263 - Daily Routine Dedupe Guard
- Fixed mobile daily routine double-injection during auto-unlock/cloud-pull races.
- Removed the separate mobile.html manual routine injector; mobile now uses the idempotent diet-mobile reconciliation path only.
- Added routine duplicate cleanup before mobile Diet render/add, with inventory restoration for extra duplicate routine entries.
- Added desktop addFoodItem routine guard so PC scheduled routine timeouts cannot duplicate the same routine item.
- Cache bumped to 263.

PEGASUS 262 - EXACT MOBILE GHOST BOUNDARY

- Mobile-only shell refinement after v261.
- Keeps the corrected TOP zone behavior.
- Removes the large blank safety gap above the fixed ghost buttons.
- The middle scroll zone is now measured dynamically from the bottom of the Back/header area to the exact top of the ghost navigation.
- Content no longer falls under the Back button or ghost buttons, but it reaches down to the point where the ghost buttons begin.
- No desktop changes.
- Cache bump to 262.

PEGASUS 261 - MOBILE THREE-ZONE SHELL LAYOUT

- Added mobile/shell-layout-mobile.js as a global mobile shell layout manager.
- Mobile views are now split into three stable zones:
  TOP: Back/header area stays fixed.
  MIDDLE: module content scrolls with touch only.
  BOTTOM: ghost buttons stay fixed and content never falls underneath them.
- Applies to existing and dynamically registered mobile modules via MutationObserver.
- TV module content no longer scrolls under the Επιστροφή button or ghost buttons.
- Desktop unchanged.
- Cache bump to 261.

PEGASUS 260 - MOBILE TV PICKS BY CHANNEL + FULL DESCRIPTIONS
- Mobile TV remains Athinorama-only and shows only “Τι αξίζει να δεις σήμερα”.
- Day picks are now grouped per channel, so each channel shows all parsed Athinorama recommendations instead of one flat mixed list.
- Removed description truncation: each film/recommendation shows the full description available from Athinorama.
- Cache key bumped so old shortened TV cache does not remain visible.
- Desktop unchanged.

PEGASUS 259 - MOBILE TV PICKS ONLY
- Mobile TV module now shows only Athinorama “Τι αξίζει να δεις σήμερα”.
- Removed the “Τώρα παίζει” channel programme cards and next-program schedule from the visible TV view.
- Keeps refresh-on-open behavior: every time the TV module is opened, it refreshes Athinorama picks.
- Parses all Athinorama day picks it can read, including channel/time and descriptions when available.
- Maintains clean mobile-only UI and no desktop changes.
- Cache bump to 259.

PEGASUS 258 - MOBILE TV REFRESH ON OPEN

- Mobile TV now refreshes from Athinorama every time the Τηλεόραση module is opened.
- Removed the startup network refresh for TV, so mobile boot stays faster.
- Cached TV data can still be shown as a temporary preview until the open-triggered refresh completes.
- The title “📺 Τηλεόραση σήμερα” still works as a manual refresh trigger.
- Athinorama-only, mobile-only, no desktop changes.
- Cache bump to 258.


PEGASUS 257 - MOBILE TV ALL ATHINORAMA DAY PICKS

- Mobile TV now shows all available Athinorama day picks instead of limiting the list.
- Removed the 18/24 global cap from the Ταινίες/Επιλογές ημέρας section.
- Removed per-channel Athinorama pick caps so all parsed choices can flow into the clean list.
- If official Athinorama picks are unavailable, the module falls back to all movie-like programmes found for the day.
- Still mobile-only, Athinorama-only, no extra source buttons, no desktop changes.
- Cache bump to 257.


PEGASUS 256 - MOBILE TV CLEAN HEADER / MORE PICKS
- Mobile TV only: removed visible ΑΝΑΝΕΩΣΗ and ΑΘΗΝΟΡΑΜΑ buttons.
- The “📺 Τηλεόραση σήμερα” title now performs refresh when tapped.
- Hidden the visible source/status line (Πηγή: Athinorama Reader...).
- Expanded day highlights so Ταινίες ημέρας / Επιλογές ημέρας shows more items when available.
- Athinorama remains the only TV source/link; desktop unchanged.
- Cache bump to 256.

PEGASUS 255 - Minimal Mobile TV View + Movie Descriptions
- Cleaned mobile/tv-mobile.js so the TV module no longer shows the separate MEGA/ANT1/ALPHA/STAR/SKAI/OPEN chip list.
- Removed the explanatory “clean view” subtitle from the TV hero for a cleaner mobile screen.
- Kept Athinorama as the only visible TV source.
- Replaced the per-channel “best choices” block with a cleaner “Ταινίες ημέρας / Επιλογές ημέρας” highlight list.
- Day movies/highlights now show longer descriptions when Athinorama provides them.
- Removed per-card guide buttons; the single Athinorama button remains at the top/fallback.
- Mobile only. Desktop unchanged.

PEGASUS 254 - Athinorama-only Mobile TV Guide
- Updated mobile/tv-mobile.js so the TV module uses only Athinorama as the visible guide source.
- Removed Digea, Programma Tileorasis, Zappit, and XMLTV fallback attempts/buttons from the mobile TV module.
- Channel guide buttons now open Athinorama programme instead of individual channel/external sources.
- Kept the clean Pegasus TV cards and last-module ordering.
- Desktop unchanged.
- Cache bumped to v254.

PEGASUS 253 - Quiet Mobile TV Guide Fallback
- Updated mobile/tv-mobile.js to stop noisy Console warnings when Athinorama/XMLTV sources are blocked by CORS/HTTP 451.
- If live EPG cannot be fetched, the TV module now stays clean: Pegasus channel cards remain visible and a clear green notice explains that live data needs an external guide/proxy.
- Added direct guide buttons for Athinorama, Digea, Programma Tileorasis and Zappit without exposing technical error text in the UI.
- The module is ready for a future worker/proxy while staying usable now as a clean TV guide launcher.
- Mobile only. Desktop unchanged.
- Cache bumped to v253.

PEGASUS 252 - Clean Mobile TV Cards
- Reworked mobile/tv-mobile.js presentation into clearer Pegasus-style mobile cards.
- TV module now separates: channel chips, "Τώρα παίζει", "Μετά", and "Καλύτερες επιλογές ημέρας ανά κανάλι".
- Added progress bar for current programme and compact per-channel daily picks.
- Technical/fallback guide links stay hidden unless loading fails or user taps Athinorama.
- New modular order rule remains: newly registered modules stay last.
- Mobile only. Desktop unchanged.
- Cache bumped to v252.

PEGASUS 251 - Mobile TV Athinorama parser
------------------------------------------------------------
- TV mobile module now tries Athinorama first.
- Parses Athinorama full channel programme for MEGA, ANT1, ALPHA, STAR, ΣΚΑΪ, OPEN.
- Parses Athinorama "TV - Αξίζει να δείτε" as best daily choices per channel.
- Keeps XMLTV as fallback if Athinorama/CORS/Reader fails.
- New modular tiles still register last.
- Desktop unchanged.
- Cache bumped to v251.


PEGASUS 250 - Mobile TV EPG polish + modular order rule
- Mobile-only update. Desktop UI unchanged.
- New modular tiles now stay at the end of the extended modular list instead of jumping forward by sortOrder.
- Reworked mobile/tv-mobile.js into six-channel TV cards: MEGA, ANT1, ALPHA, STAR, SKAI, OPEN.
- TV module now tries to load XMLTV EPG and show what plays now plus best daily picks per channel.
- Added fallback guide buttons for Digea/Programma Tileorasis/Zappit if live EPG is blocked by CORS/network.
- Cache bumped to v250.

PEGASUS 248 - MOBILE FINANCE SIMPLE AMOUNT MODE

Mobile-only finance patch:
- Removed the transaction description input from Οικονομικά.
- Finance now asks only for amount and uses the existing + Έσοδο / - Έξοδο buttons.
- Validation now requires only a valid positive amount.
- History displays transaction type (Έσοδο/Έξοδο), date, amount and delete button; old saved descriptions are ignored visually.
- Desktop UI unchanged.
- Cache/storage version bumped to 248.

PEGASUS 247 - MOBILE GHOST BUTTON POSITION FIX
- Restores the ghost navigation buttons to their lower fixed position.
- Keeps the mobile scroll boundary ending at the beginning of the ghost buttons.
- Moves the clearance from body padding to the scrollable view area so the buttons do not visually rise.
- Cache/storage version bumped to 247.

PEGASUS 246 - MOBILE GHOST SCROLL BOUNDARY FIX
- Mobile-only adjustment. Desktop UI unchanged.
- Tightened the scrollable content boundary so each mobile view reaches down to the start of the ghost buttons instead of stopping too high above them.
- Reduced extra bottom padding on scroll containers while preserving hidden scrollbars and hand scrolling.
- Cache/storage version bumped to 246.

PEGASUS 245
- Mobile-only update.
- Targets exercise thumbnails now open a large zoom preview when tapped.
- Added full-screen Pegasus styled image zoom overlay with close button and tap-outside close.
- Applied mobile ghost-safe scroll policy: content scrolls by touch, no visible scrollbar, and the scroll area stops above the ghost navigation buttons.
- Added sticky mobile top controls/header/back button behavior while content scrolls.
- Desktop UI unchanged.
- Cache bump to 245.

PEGASUS 243 - Biometric HTTPS Safe Mode
- mobile/biometric-unlock-mobile.js updated to v1.3.243.
- Biometric unlock now stays dormant on non-HTTPS / non-secure origins instead of showing disruptive errors.
- Fingerprint/WebAuthn still requires HTTPS or localhost because Android/Chrome enforces this; Pegasus cannot bypass it safely.
- Service worker registration now runs only on secure context to avoid HTTPS-related registration errors on plain HTTP/file origins.
- Service worker cache bumped to 243.

PEGASUS 240 - Mobile Biometric Unlock Module

- Added new separate file: mobile/biometric-unlock-mobile.js.
- Mobile PIN vault can now show a fingerprint/passkey unlock button when the device is already approved.
- After a normal PIN + MASTER KEY unlock, Pegasus offers activation of Android/Chrome biometric unlock.
- Biometric unlock uses WebAuthn/platform authenticator; Pegasus never stores the fingerprint itself.
- Added a small mobile vault bridge so external modules can finish the existing PIN unlock flow safely.
- Social/Secure Vault PIN screen can also show a biometric button after biometric unlock has been enabled.
- Permanent local cache bumped to v240 and service worker registration bumped to 3.36.240.

PEGASUS 239 - Desktop Boot Supplement Sync Guard

Bugfix:
- Fixed protein/creatine refill still syncing according to the desktop when the PC opens after a mobile refill.
- Desktop boot writes for `pegasus_supp_inventory` are now blocked from entering the cloud pending queue before the first pull.
- Existing stale desktop supplement pending entries are pruned before pull/push when the cloud already has supplement inventory.
- Before any desktop push, cloudSync performs a final per-field merge with the current cloud supplement inventory so the newest mobile protein/creatine value wins.
- Permanent local cache bumped to v239 and service worker registration bumped to 3.36.239.

Expected behavior:
- Refill protein on mobile to 2500g.
- Open desktop after sync.
- Desktop first pulls/merges, then shows 2500g and cannot push its stale old stock back to cloud.

PEGASUS 238 - Supplement Refill Sync Timestamp Fix

Bugfix:
- Fixed mobile supplement refill being overwritten by the desktop's older protein/creatine value.
- `pegasus_supp_inventory` now carries per-field timestamps: `protUpdatedAt` and `creaUpdatedAt`.
- Cloud sync now merges protein and creatine stock by newest field update instead of blindly replacing the object.
- Mobile refill, mobile consume/restore, PC consume/restore/restock, and PC inventory guard preserve legacy keys while updating sync metadata.
- Permanent local cache bumped to v238 and service worker registration bumped to 3.36.238.

Expected behavior:
- Refill protein on mobile to 2500g.
- Open desktop after sync. Desktop should keep 2500g instead of reverting to its old local value.
- Future whey/creatine consumption and restore operations should sync in the correct direction by latest edit.

PEGASUS 237 - Morning Report Full Backup

Problem fixed:
- The user wants the daily morning report to also send a complete backup of PEGASUS data so future data loss can be recovered from the email.

Changes:
- Added PEGASUS 237 Daily Backup Collector in backup.js.
- Morning report now builds a fresh backup at send time, not the previous evening.
- Backup is injected into existing EmailJS template data through nutrition_advice so it is sent even if the EmailJS template has not been edited to add a new backup field.
- Additional params are also filled: backup_summary, backup_json, pegasus_backup_json, backup_schema, backup_key_count, backup_size_bytes, backup_truncated.
- Backup includes PEGASUS user data keys, food logs, cardio logs, weight keys, Kouki keys, parking, biometrics, lifting, car/service data, finance, notes, supplies, movies, YouTube, missions, social/contacts where present.
- Backup deliberately skips PIN/master/API/vault secrets, runtime logs, sync traces, old backups/snapshots and permanent-cache markers to avoid unsafe leaks and recursive oversized emails.
- Manual export filename is now PEGASUS_FULL_BACKUP_YYYY-MM-DD.json and uses the same complete user-data collector.
- Permanent local cache bumped to v237.

Install note:
Upload all files. First open after this package will build/check v237 local cache once. The next morning report should log: "PEGASUS REPORT: Daily backup attached" before sending.

Package:
- Code-only.
- No images folder.
- No videos folder.

PEGASUS 241 - Mobile Biometric Enrollment Fix
- Improved mobile/biometric-unlock-mobile.js v1.1.241.
- Biometric enrollment prompt now appears after manual unlock and approved-device auto unlock.
- Added persistent Settings card: ΒΙΟΜΕΤΡΙΚΟ Ξεκλείδωμα, with enable/test/reset controls.
- PIN modal now always shows biometric unlock when enrolled.
- Added PegasusBiometricUnlock.status(), showPrompt(), reset() helpers for console debugging.
- Service worker cache bumped to 241.


PEGASUS 242 - Mobile Load Fix for Biometric Module
- Fixed mobile load/freeze caused by biometric settings card re-render loop.
- mobile/biometric-unlock-mobile.js updated to v1.2.242 with render guard and throttled MutationObserver.
- Keeps separate biometric module and settings option, but prevents continuous DOM rewrites.
- Service worker cache bumped to 242.

PEGASUS 244
- Mobile-only UI improvement: added small exercise thumbnails in the Mobile Στόχοι panel for Ασκήσεις Ημέρας and Τελευταία αποθηκευμένα κιλά.
- Uses existing deployed /images/ assets via ../images/ on mobile. No images folder or new media assets are bundled.
- Added Pegasus-style fallback placeholder when an exercise image is missing.
- Desktop UI unchanged.
- Cache bump to 244.



PEGASUS 249 - MOBILE TV PROGRAM MODULE
---------------------------------------
- Added new separate mobile module: mobile/tv-mobile.js.
- Adds a Τηλεόραση tile in the mobile modular grid.
- Shows quick TV-guide source buttons and an in-app external EPG viewer.
- Includes Chrome/open fallback for sources that block iframe embedding.
- Stores only the selected TV source preference; no TV listings are stored.
- Desktop UI unchanged.
- Service worker cache bumped to 249 and tv-mobile.js added to offline asset list.


PEGASUS 264 - Mobile TV Live Refresh No Cache
- Mobile-only TV patch. Desktop unchanged.
- The Τηλεόραση module now fetches fresh Athinorama daily picks every time it opens.
- Cached/stale TV data is no longer rendered on boot or used as fallback.
- Opening Τηλεόραση shows a live refresh message, then today’s Athinorama picks grouped by channel.
- If Athinorama is unavailable, the module shows a clean notice instead of old cached picks.
- tv-mobile.js bumped to v2.2.264 and mobile script query updated to ?v=264.
- Service worker cache bumped to 264.


=== PEGASUS 268 - Silent Mobile Credentials + Auto Parking GPS ===
- Removed custom popup prompts for saving the mobile MASTER KEY.
- After a successful mobile unlock, the MASTER KEY is saved silently and locally on that device; PIN is never saved and nothing is uploaded to cloud.
- Removed the custom Pegasus popup for local phone location; Android/Chrome permission prompt is enough.
- Parking mobile now captures GPS automatically every time the Parking category opens.
- Parking keeps the last 5 positions so accidental opens do not lose the previous parking spot.
- Parking entries include map buttons and accuracy/time when available.
- Parking location/history are treated as local-only and are not exported to cloud.
- No new JS files were added; changes are integrated in existing mobile/mobile.html, parking-mobile.js and cloudSync.js.
- Cache bump to 268.

=== PEGASUS 269 - Parking Street Labels + Direct Maps ===
- Parking mobile now tries to convert GPS coordinates into a readable street/address label using reverse geocoding.
- The Parking tile and history prefer street/address text instead of raw coordinate numbers.
- The last 5 parking history items keep the coordinates internally for maps, but display the street when available.
- Map buttons now try to open the native Android maps handler directly with a geo: link, falling back to Google Maps web only if needed.
- Removed the local last-location success text after manual location save; the UI updates silently.
- Parking address cache remains local-only and is excluded from cloud sync.
- No new JS files were added; changes are integrated in existing parking/mobile/sync files.
- Cache bump to 269.


PEGASUS 270 - Mobile Parking Static Map Preview
- Added direct static map preview inside Parking mobile using the saved GPS position.
- Parking now shows a map image with a pin for the current saved position instead of relying only on opening an external map.
- The last 5 parking history entries now show compact map thumbnails when GPS coordinates are available.
- Street/address labels remain local-only and continue to use the previous reverse-geocoding logic.
- External map opening remains available by tapping the map image/history card.
- No desktop changes.


PEGASUS 271 - Mobile Parking Map 500 Fix
- Replaced staticmap.openstreetmap.de static map images with direct OpenStreetMap tile preview.
- Fixes HTTP 500/server error shown when opening Parking map previews.
- Parking still shows a visual map preview with centered pin and 5-place history thumbnails.
- No new files. Mobile only. Desktop unchanged.


PEGASUS 272 - Mobile Parking Clean Text + Manual Area
- Reverted Parking mobile to a clean text-based view without map images/thumbnails.
- Removed the OpenStreetMap tile preview after map image/server issues.
- Parking still captures GPS automatically and keeps the latest 5 local-only positions.
- Reverse geocoding still tries to show a street/area name instead of coordinates.
- Added manual area/street entry: if the street name is not found, the user can write the location manually while keeping the saved GPS coordinates internally.
- History now shows numbered clean cards with street/area, time/accuracy, and a direct map button.
- Mobile only. Desktop unchanged. No new files.
- Cache bump to 272.

PEGASUS 274 - Mobile Parking Manual Save Only
- Parking no longer captures GPS automatically when the Parking category opens.
- This prevents accidental overwriting of the real car location when the user only opens Parking to check the saved place.
- Parking remains coordinate-based, with no street lookup, no map preview, and no manual area field.
- The user saves a new position only by pressing ΑΠΟΘΗΚΕΥΣΗ ΘΕΣΗΣ.
- The last 5 local-only parking positions remain available in history.
- The Χάρτης button still opens Google Maps web directly.
- Mobile only. Desktop unchanged. No new files.
- Cache bump to 274.


PEGASUS 276 - Mobile Parking Street/Number Labels
- Parking remains manual-save only: opening Parking does not overwrite the saved car position.
- Saved GPS coordinates are converted to a readable street/address label when possible.
- The Parking current card and 5-place history prefer street + number / street name instead of raw coordinates.
- If reverse geocoding is unavailable, Parking safely falls back to coordinates.
- Coordinates are still kept internally for the Χάρτης button and map navigation.
- Χάρτης keeps the direct Google Maps app intent behavior from 275.
- Mobile only. Desktop unchanged. No new files.
- Cache bump to 276.

PEGASUS 277 - Mobile Parking Geo Map Restore + Stable Map Labels
- Restored Android geo: map opening for Parking so the Χάρτης button opens the installed map handler directly instead of routing through Google Play/Google Maps web pages.
- Removed the automatic timed web fallback that could steal focus back from Google Maps or show the Play Store page.
- Parking remains manual-save only: opening Parking does not overwrite the saved car position.
- Street/address labels from v276 remain, with coordinate fallback if reverse geocoding is unavailable.
- The last 5 parking history buttons now show the fixed text “Χάρτης” instead of all-caps “Χάρτης”.
- Added stronger mobile CSS so Parking map button text stays small/stable and is not changed by adaptive typography refreshes.
- Mobile only. Desktop unchanged. No new files.
- Cache bump to 277.

PEGASUS 278 - Parking OpenStreetMap no-Play map + fixed label
- Parking Χάρτης no longer uses Google Maps/geo intent fallback that can route APK/WebView to Google Play.
- Parking Χάρτης now opens an OpenStreetMap web map directly for the saved coordinates.
- Current and history map buttons are hard-locked to the label “Χάρτης”.
- Added a MutationObserver guard so adaptive typography/label polish cannot flip the text to uppercase every second.
- Parking still does not auto-save location on open; save remains manual.
- Desktop unchanged.


PEGASUS 279 - Parking v269 direct map open restore
- Restored the v269 map-opening flow for Parking: Android geo: first, Google Maps web fallback only if the page did not leave/hide.
- Kept the current safe Parking behavior: no auto-save on open, manual save only, 5-location history, address label when available.
- Kept the stable “Χάρτης” button text and fixed font so adaptive typography does not change it every second.
- Desktop unchanged.

PEGASUS 281 - Parking Strict Street Labels
- Parking keeps the v269/v279 map-opening flow that worked best on the user's APK/device.
- Reverse geocoding is now strict: it shows a label only when a real street/road is found.
- Broad locality-only labels like “Ιωάννινα” are no longer accepted as the car position.
- If no street is available, Parking safely falls back to coordinates instead of showing a misleading city name.
- Older saved broad labels are rechecked and either upgraded to street/number or hidden back to coordinates.
- The “Χάρτης” label remains fixed/small in the 5-location history.
- Mobile only. Desktop unchanged. No new files.
- Cache bump to 281.

PEGASUS 286
- Mobile cleanup pass.
- Removed the mobile TV module completely because Athinorama fetch is not reliable enough in production.
- Removed the last phone location card/helper from Settings & Weight.
- Centered the create buttons in Supplies, Movies, Activities/Missions, Body measurements and Maintenance.
- Removed per-module Backup/Restore buttons from Documents and Vehicle.
- Added a central Data Management section with three cards:
  * Local data backup/restore
  * Cloud data backup/restore
  * Full local + cloud backup/restore
- Kept desktop unchanged.


PEGASUS 287 - Safe Merge Restore
- Data Management restore now uses Safe Restore / Merge instead of blind overwrite.
- Local restore creates an emergency backup first, then merges missing fields, arrays and history items while preserving existing newer/current values.
- Cloud restore reads the current cloud first, merges it with the backup, clears delete markers and writes the merged record.
- Full restore applies safe merge for both local and cloud data.
- Restore buttons now say “Ασφαλής επαναφορά”.
- Desktop unchanged. No new files.
- Cache bump to 287.

PEGASUS 288 - Mobile restore no Save As
- Fixed mobile Data Management restore flow that could show an Android “Save as” dialog immediately after selecting a restore file.
- Emergency backup before restore is now stored silently in localStorage/sessionStorage instead of being downloaded during restore.
- Restore buttons now use explicit hidden file inputs instead of label-wrapped file controls for cleaner Android/WebView behaviour.
- Safe Merge Restore logic remains unchanged.
- Desktop unchanged. No new files.
- Cache bump to 288.

PEGASUS 289 - Static label case cleanup
- Converted static uppercase UI labels to sentence-style casing where safe.
- Removed adaptiveTypography.js from desktop/mobile loading and service worker cache.
- Preserved internal storage keys and lifter identifiers to avoid data loss.
- Cache bump to 289.

PEGASUS 290 - Remove unused voice module
- Removed the unused voice.js module because it was not loaded by desktop/mobile UI and never spoke in normal use.
- Removed voice.js from service worker cache and desktop fallback asset list.
- Removed the dead PegasusVoice bridge from aiHandler.js and cleaned the voice-enabled log text.
- No layout, sync, data, workout, diet or mobile module behavior changed.
- Cache bump to 290.
